<%= form_with(model: size, local: true) do |form| %>

<% if @size.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@size.errors.count, "Error") %> prohibited this form from being saved:</h2>
 
    <ul>
    <% @size.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
    <% end %>

    <div>
        <%= form.label :size %>
        <%= form.text_field :size, :required => true, :autofocus => true, :placeholder => "Which size would you like to add?"%>
    </div>
    <div>
        <%= form.submit %>
    </div>
<% end %>