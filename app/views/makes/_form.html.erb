<%= form_with(model: make, local: true) do |form| %>

<% if @make.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@make.errors.count, "Error") %> prohibited this form from being saved:</h2>
 
    <ul>
    <% @listing.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
    <% end %>

    <div>
        <%= form.label :make %>
        <%= form.text_field :title, :required => true, :autofocus => true, :placeholder => "Which make would you like to add?"%>
    </div>
    <div>
        <%= form.submit %>
    </div>
<% end %>

